#!/srv/webtools/webtools/libcchdoenv/bin/python

from __future__ import with_statement
import sys

import libcchdo as L
import libcchdo.model.datafile
import libcchdo.formats.summary.hot as sumhot
import libcchdo.formats.summary.woce as sumwoce


def main(argv):
    if len(argv) < 2:
        print 'Usage:', argv[0], '<HOT sumfile>'
        return 1
    
    with open(argv[1], 'r') as in_file:
        file = L.model.datafile.SummaryFile()
        sumhot.read(file, in_file)
        sumwoce.write(file, sys.stdout)


if __name__ == '__main__':
    sys.exit(main(sys.argv))
